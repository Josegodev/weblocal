/* styles.css */
body { font-family: Arial, sans-serif; background-color: #1890FF; color: white; }
h1, h2, h3 { margin-bottom: 1rem; }
p { line-height: 1.5; }
pre { white-space: pre-wrap; }
code { background-color: #D1F8DA; padding: .2em; border-radius: 4px; color: green; }